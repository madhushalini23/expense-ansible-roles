server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://backend.gundluru.online:8080/;
        proxy_http_version 1.1;
    }

    location /health {
        stub_status on;
        access_log off;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}